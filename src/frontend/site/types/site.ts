export type NavigationItem = {
  label: string;
  href: string;
};

export type Category = {
  name: string;
  description: string;
  action: string;
  href: string;
};

export type SocialLink = {
  label: string;
  href: string;
};